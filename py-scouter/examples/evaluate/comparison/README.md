# Comparison Evaluation

This directory shows how to compare two agents on the same scenarios and tasks.

Current example:

- `google_vs_openai/evaluate.py`

It reuses the non-interactive shared scenarios/profile and reports pass-rate deltas with `compare_to()`.

