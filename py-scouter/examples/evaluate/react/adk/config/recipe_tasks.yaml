tasks:
  - task_type: Assertion
    id: response_not_empty
    context_path: response
    operator: IsString
    expected_value: true
    description: Each recipe agent response is a non-empty string
    depends_on: []
    condition: false
