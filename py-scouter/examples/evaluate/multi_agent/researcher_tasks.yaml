tasks:
  - task_type: Assertion
    id: researcher_response_is_string
    context_path: response
    operator: IsString
    expected_value: true
    description: "Researcher output is a non-empty string"
    depends_on: []
    condition: false
